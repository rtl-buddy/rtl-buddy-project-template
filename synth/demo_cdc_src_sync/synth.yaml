rtl-buddy-filetype: synth_config

# Tech-independent Yosys synthesis. Proves the source-synchronous
# clock-forwarding chain elaborates and synthesizes cleanly — the
# real cells produced for the per-block dividers are what give each
# forwarded clock a distinct net identity for CDC lint.
syntheses:
  - name: "demo_cdc_src_sync_synth_generic"
    desc: "Tech-independent Yosys synthesis of demo_cdc_src_sync_top"
    model: "demo_cdc_src_sync_top"
    model_path: "../../design/demo_cdc_src_sync/models.yaml"
    tool: "yosys"
    reglvl: 0
