// vim: set ts=2 sw=2 et :
//
// Header pulled in via `+incdir+.` from a filelist. The include path
// is what forces the `+incdir+` filelist entry to matter — see
// `test_modules.f` in this directory and `models.yaml`.

`ifndef DEMO_SYNTH_INCDIR_DEFS_SVH
`define DEMO_SYNTH_INCDIR_DEFS_SVH

`define DEMO_SYNTH_INCDIR_W 8

`endif
