# sandbox Verification

This directory contains the sandbox/example verification suite used for basic
`rtl-buddy` and SystemVerilog flow examples.

The runnable tests are defined in [tests.yaml](./tests.yaml), and the suite
intent is documented in [testplan.md](./testplan.md).
