[project]
name = "rtl-buddy-project-template"
version = "0.0.0"
requires-python = ">=3.11,<3.12"
dependencies = [
  "rtl_buddy",
  "cocotb==2.0.1",
  "pytest==8.4.2",
]

[tool.uv]
package = false

[tool.uv.sources]
rtl_buddy = { git = "https://github.com/rtl-buddy/rtl_buddy.git" }
